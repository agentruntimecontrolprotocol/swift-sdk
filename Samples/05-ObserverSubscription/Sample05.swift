import ARCP

@main
struct Sample05ObserverSubscription {
    static func main() async {
        print("Sample 05 — observer subscription (wire \(ARCPVersion.wire))")
        print("Implementation lands in Phase 5.")
    }
}
