# Cost Budget

Demonstrates `cost.budget` enforcement. The tool charges `USD` three
times against a `USD:0.10` lease, and the third charge surfaces
`BUDGET_EXHAUSTED`.

```sh
swift run CostBudget
```
